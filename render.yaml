services:
  - type: web
    name: memorycare-backend
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node app-server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
